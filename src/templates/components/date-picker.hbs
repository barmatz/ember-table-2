<div class="input-group">
  {{input class="form-control" value=value placeholder=placeholder}}
  <span class="input-group-btn">
    <button class="btn btn-link" {{action "toggle"}}><i class="fa fa-calendar"></i></button>
  </span>
</div>